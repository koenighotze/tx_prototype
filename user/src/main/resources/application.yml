#debug=true

server:
  port: 8087
#logging.file=user.log

spring:
  data:
    mongodb:
      host: 192.168.99.100
      database: user
      port: 27017

logging:
  level:
    org:
      springframework:
        web:
          client: DEBUG

security:
  user:
    name: user
    password: user